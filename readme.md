A simple python script to prove if functions are odd, even or neither

Currently does not support composite functions.

Requires a bit of fiddling with variables and powers, but is workable

"*" = multiplication
"^" = indicies
"/" = division
"+" = addition
"-" = subtraction